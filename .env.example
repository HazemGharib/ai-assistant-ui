# UI environment (Phase 1)

# Integrated: point at backend chat API
VITE_BACKEND_BASE_URL=http://127.0.0.1:3001

# Isolated: leave VITE_BACKEND_BASE_URL unset to use localAgentAdapter
# No secrets required — placeholders only.
