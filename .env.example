# Local UI env (no secrets). Copy to `.env.local` as needed.
# Isolated mock mode (default when unset or true):
VITE_USE_MOCK_BACKEND=true
# Integrated mode — point at local backend only (disable mock):
# VITE_USE_MOCK_BACKEND=false
# VITE_BACKEND_BASE_URL=http://127.0.0.1:3001
