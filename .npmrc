# @hazemgharib packages resolve from GitHub Packages.
# Auth locally via ~/.npmrc: //npm.pkg.github.com/:_authToken=<PAT with read:packages>
# CI auth is provided by actions/setup-node + NODE_AUTH_TOKEN.
@hazemgharib:registry=https://npm.pkg.github.com
